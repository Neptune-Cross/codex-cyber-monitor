# Codex Cyber 实时监测器 1.1

## 软件行为

- 常驻 Windows 当前用户的托盘区。
- 登录 Windows 时自动启动。
- 增量读取 `%USERPROFILE%\.codex\sessions\**\rollout-*.jsonl` 和 `%USERPROFILE%\.codex\archived_sessions\**\rollout-*.jsonl`。
- 仅按结构化 JSON 字段判断，不全文搜索用户请求。
- 捕获 Cyber 事件后立即弹出红色、持续置顶的前台警告窗。
- 警告窗不设倒计时，仅在用户点击关闭按钮、标题栏关闭按钮或按 `Alt+F4` 后关闭。
- 同一时间只显示一个警告窗；新事件会追加到现有窗口。
- 未确认警告保存在本地状态文件中；程序异常退出或重新登录后会继续显示。
- Win+D、会话解锁或窗口被最小化后，未确认警告会自动恢复可见。
- 首次运行把已完整写入的旧日志建立为历史基线，从当前正在写入的请求和之后的新请求开始告警。
- 监测面板会在后台扫描并显示 `sessions` 与 `archived_sessions` 中的全部历史 Cyber 记录；旧记录只进入列表，不弹红窗。
- 历史列表支持刷新、双击查看详情以及右键复制完整记录。

## 捕获条件

| 结果 | 精确条件 |
|---|---|
| `CYBER_BLOCK` | `task_complete` 且 `error.codex_error_info == cyber_policy` |
| `CYBER_REROUTE` | `model_reroute` 且 `reason == high_risk_cyber_activity` |
| `CYBER_VERIFICATION` | `model_verification.verifications[]` 包含 `trusted_access_for_cyber` |
| `CYBER_BUFFERING` | `safety_buffering.use_cases[]` 包含 `cyber` |

## 托盘操作

软件图标可能位于任务栏右侧 `^` 的隐藏图标区内：

- 左键：优先显示当前红色警告，否则打开监测面板。
- 右键：打开监测面板、查看全部历史记录、测试警告窗、打开日志、切换开机启动或退出。
- 正常监测为绿色盾牌；存在未确认警告时变为红色盾牌；监测异常时显示橙色盾牌。

## 隐私日志

日志目录：

```text
%LOCALAPPDATA%\CodexCyberMonitor\logs
```

日志不保存 prompt、回复内容、原始 JSONL 或 `error.message`。`turn_id` 和源路径使用安装级随机盐进行 HMAC 短摘要，只保留可关联的匿名引用。

待确认状态与增量游标位于：

```text
%LOCALAPPDATA%\CodexCyberMonitor\state
```

## 安装

在发布目录中执行：

```powershell
$ErrorActionPreference = 'Stop'
.\安装并启动.ps1
```

默认安装至：

```text
%LOCALAPPDATA%\Programs\CodexCyberMonitor
```

## 卸载

```powershell
$ErrorActionPreference = 'Stop'
& "$env:LOCALAPPDATA\Programs\CodexCyberMonitor\卸载.ps1"
```

连同隐私日志和游标数据一起删除：

```powershell
$ErrorActionPreference = 'Stop'
& "$env:LOCALAPPDATA\Programs\CodexCyberMonitor\卸载.ps1" -RemoveData
```

## 判定边界

红窗表示本地捕获到了结构化 Cyber 事件。它不是账号 strike 数量，也不等同于封禁通知。
